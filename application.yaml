apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-local
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ManuLeitao/test-argoCD.git
    targetRevision: main
    path: gitops-app/overlays/local
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-gitops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true